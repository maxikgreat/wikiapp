import React, {Dispatch, SetStateAction, VFC} from 'react';import {ActiveTip, Tip} from '../App';interface TipsProps {	tips: Tip[],	activeTip: Tip | null,	setActiveTip: Dispatch<SetStateAction<ActiveTip>>}export const Tips: VFC<TipsProps> = ({ tips, activeTip, setActiveTip }) => {	return (		<div className="list-group">			{tips.map(tip => (				<button					key={tip.pageid}					type="button"					className={`list-group-item list-group-item-action ${tip.pageid === activeTip?.pageid && 'bg-success text-white'}`}					onClick={() => setActiveTip((prevState => ({...prevState, tip})))}				>					{tip.title}				</button>			))}		</div>	);};