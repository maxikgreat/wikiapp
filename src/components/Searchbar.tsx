import {Dispatch, SetStateAction, VFC} from 'react';import {Tip} from '../App';import {Loader} from '../components/Loader';interface SearchbarProps {	search: string,	setSearch: Dispatch<SetStateAction<string>>,}export const Searchbar: VFC<SearchbarProps> = ({ search, setSearch }) => {	return (		<form autoComplete="off" className="search-bar flex-column align-items-center">			<div className="d-flex">				<input					value={search}					onChange={({ target: { value }}) => setSearch(value)}					type="search"					name="search"					pattern=".*\S.*"					required				/>				<button className="search-btn" type="submit">					<span>Search</span>				</button>			</div>		</form>	);};